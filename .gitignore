.env
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Experiment outputs. Results are JSON, so an untargeted *.json rule would also
# swallow them; ignore the directories instead.
results/
results_demo/
docs/experiment_results.md

# LaTeX build artefacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.pdf
paper/*.fdb_latexmk
paper/*.fls
paper/*.synctex.gz
